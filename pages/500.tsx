import React, { FC } from 'react';

import utilStyles from '../styles/utils.module.css';

const Custom500: FC = () => {
  return <h1 className={utilStyles.headingXl}>500 Internal Server Error</h1>;
};

export default Custom500;
